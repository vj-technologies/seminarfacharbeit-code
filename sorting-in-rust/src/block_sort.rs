pub fn block_sort(array: &mut Vec<i32>) {
    /* Block sort pseudo code
     * split array into 2 halfs
     * for each half: split into blocks of size sqrt(half size)
     * rotation based merging (uses binary search)
     * merge into mega block
     */
}
